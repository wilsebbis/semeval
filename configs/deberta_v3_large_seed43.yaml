# CLARITY Training Config: DeBERTa-v3-large — Seed 43
# For 3-seed ensemble. Run after seed 42.

model_name: microsoft/deberta-v3-large
task: evasion
max_length: 384
precision: bf16
batch_size: 8
grad_accum: 2
lr: 1.0e-5
weight_decay: 0.01
warmup_ratio: 0.06
epochs: 5
patience: 3
dropout: 0.1
seed: 43
output_dir: checkpoints/deberta_v3_large_seed43
use_focal_loss: false
use_class_weights: false
label_smoothing: 0.05
alpha: 0.7
consistency_beta: 0.1
fp16: false
